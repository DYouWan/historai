# 多关键帧与镜内 Seedance 规格（已定稿）

本文档记录产品与设计共识，**后续开发以本文为准**。若变更某条，请在本文件修订并注明日期。

---

## 1. 目标与背景

- **现状**：一分镜默认对应一张主静图；图生视频（含首尾帧）时，**镜与镜之间**或单镜内变化过大，衔接困难。
- **目标**：在**不改变「一镜 = 一条口播」**的前提下，由模型按分镜内容建议每镜 **K 个关键帧（静图）**，镜内 **K−1 段** Seedance（或其它图生视频）文案，用于相邻关键帧之间的受控过渡。
- **原则**：完整实现、与现有分镜扩写流程分层；默认 **K=1** 时行为与当前产品一致。

---

## 2. 核心定义

| 概念 | 定义 |
|------|------|
| **分镜（一镜）** | **一条口播**为一个叙事单元；多关键帧、多段视频均挂在该单元下，不把一条口播拆成多条分镜。 |
| **第 N 镜** | 与分镜列表顺序 **严格一一对应**；仅在该镜内部增加子结构，**不重排镜号**。 |
| **关键帧** | 镜内多张静图的用户可见名称；**关键帧 1 = 主静图位**（列表主视觉、导出主图字段）。 |
| **K** | 单镜关键帧数量，**整数，K∈[1,4]**。 |

---

## 3. 关键帧数量与决策

- **默认**：未做多关键帧规划时 **K=1**；全局**不默认**把所有人改为 K>1。
- **决策方式**：**模型提议 K**，程序 **clamp 到 [1,4]**；非法值（0、负数、>4、非整数）由程序修正。
- **确认 UX**：**不弹窗确认 K**；在进度或结果区展示本镜 **K** 与镜内段数即可。

---

## 4. 能力范围与连贯性

- **能力**：多关键帧能力 **全局具备**（不单独拆「仅 Seedance 路径才多帧」的另一套数据模型）。
- **镜内画面约束**（与现有分镜/人物连贯 **同级或更严**）：
  - 人物外观、时代、服饰与主静图一致；镜内禁止无依据换脸、换朝代、换全套造型。
  - **同一连续时空**；允许景别、机位、姿态、小道具的 **渐进变化**；禁止无交代跳到另一地点/另一昼夜。
- **关键帧 1 与已有主图**：
  - 从 **K=1 扩到 K>1** 时：**默认保留当前主图为关键帧 1**，其后 **追加** 关键帧 2…K 及段间文案/生成。
  - 提供 **「整镜关键帧全部重规划并重出图」**（需防误触确认），此时 1…K 均按新规划处理。

---

## 5. Seedance / 图生视频形态

| 场景 | 约定 |
|------|------|
| **K≥2，镜内第 s 段** | 默认 **首尾帧**：输入 **关键帧 s** + **关键帧 s+1**，配套 Seedance 文案描述中间运动。 |
| **K=1** | **单图 + 运动/镜头描述**（或现有单图 Seedance 路径），不强行凑尾帧。 |
| **接口不支持双图** | 允许工程 **兜底**为单图+文案（文档中的「C」），不作为主产品路径依赖。 |

- **段数**：**K 张关键帧 → 镜内固定 K−1 段**；第 s 段 **只**负责 **关键帧 s → 关键帧 s+1**，不合并跨多帧的长段。
- **镜间（镜 N 末帧 → 镜 N+1 首关键帧）**：**第一版不自动生成**镜间衔接视频；硬切或外出剪辑。

---

## 6. 时长与口播

- **每段视频时长**：
  - 若存在 **镜级可用时长估算**：该镜总时长在 **K−1 段间均分**，并夹到 **API 允许的单段最短/最长**。
  - 若无可靠镜级时长：使用 **产品默认单段秒数**，同样夹到 API 范围；镜内各段可先相同默认。
  - **秒数以程序计算为准**写入调用参数；模型文案侧重运镜与节奏，不作为唯一时长来源。
- **口播**：**第一版不切分**；整段口播仍只属于该镜；镜内多段视频为 **连续画面轨**。按关键帧细拆字幕时间轴列为 **后续**能力。

---

## 7. 与分镜扩写、LLM 调用

- **阶段顺序**：**分镜扩写定稿之后**，再进入独立的 **「关键帧 + 镜内段规划」** 阶段；**不**与分镜扩写合并为同一次模型输出。
- **LLM 调用**：**每镜优先一次**结构化 JSON（含 K、各关键帧画面要点、各段 Seedance 等）；若输出过大或质量不稳，允许 **内部分拆**，对用户仍表现为一次「本镜规划」动作。

---

## 8. 界面与操作

- **展示**：主行 **口播 + 主图（关键帧 1）+ K 角标**；折叠区内 **关键帧缩略条 1…K** + **镜内段 1…(K−1)**，每段 Seedance **可编辑**；默认不铺满一屏。
- **主操作**：**「本镜一键」**——规划 K、各帧要点、各段 Seedance、为 **缺失** 关键帧出图（关键帧 1 已保留则跳过）；视频生成若重或异步，可 **排队或第二步「生成镜内视频」**。
- **子操作**：**仅重新规划**（不动已有图）、**仅补图**、**仅生成/重生成第 s 段视频**。
- **只做静图用户**：K=1 时与现网一致；K>1 也可仅作为多静图 + 文案资产，不强制走视频。

---

## 9. 失败重试与成本

- **重试**：**子项级**（某一关键帧图、某一段视频）；失败 **不**自动整镜重跑；全量重算仅由用户显式触发（如整镜重规划）。
- **上限**：**不设全片**关键帧总数或段数的硬性产品上限；保留 **每镜 K≤4**。批量任务可用队列、并发做 **工程保护**（不作为「全片张数顶」产品需求）。

---

## 10. 导出与清单

- **清单**：使用 **`manifestVersion`**（或等价版本字段）；保留 **主图/主路径** 字段（= 关键帧 1），保证只读主图的流程仍可理解。
- **扩展字段**（新读者可选）：如 `keyframes[]`、`intraShotSegments[]`（起止关键帧索引、Seedance 文案、视频路径等）。
- **目录约定**：按 **分镜** 分子路径（如 `scene-{n}/keyframe-{k}.*`、`scene-{n}/segment-{s}.*`），具体命名在实现阶段定死，要求 **可脚本遍历、镜内有序**。

---

## 11. 旧项目

- **明确不做**：**不考虑旧项目**的专门迁移、升级向导或兼容分支；新实现以 **新 schema** 为准。（若运行时缺字段，实现层可按 **K=1** 缺省，但不承诺旧工程产品级支持。）

---

## 12. 决策对照表（快速查）

| # | 结论摘要 |
|---|-----------|
| 1 | 一镜 = 一条口播；子结构挂镜下。 |
| 2 | 第 N 镜与列表顺序一一对应。 |
| 3 | 用户可见名：关键帧。 |
| 4 | K∈[1,4]，默认 1。 |
| 5 | 模型提议 K + 程序夹紧；不弹窗确认 K。 |
| 6 | 能力全局具备；默认 K=1。 |
| 7 | 镜内连贯 ≥ 现有镜间连贯。 |
| 8 | 关键帧 1 = 主静图；扩多默认保留首图；可整镜重规划。 |
| 9 | K≥2 段内默认首尾帧；K=1 单图+文案；接口兜底另议。 |
| 10 | K 帧 → K−1 段，段段相邻。 |
| 11 | 第一版无镜间自动生成衔接。 |
| 12 | 时长程序算；均分或默认 + API 夹取。 |
| 13 | 口播不切分（首版）。 |
| 14 | 主行 + 折叠 K/段编辑。 |
| 15 | 本镜一键 + 子操作。 |
| 16 | 静图用户 K=1 无感。 |
| 17 | 版本化清单 + 主图字段 + 扩展结构。 |
| 18 | 不考虑旧项目。 |
| 19 | 子项重试。 |
| 20 | 无全片上限；每镜 K≤4。 |
| 21 | 每镜优先一次合并 JSON。 |
| 22 | 扩写后再独立关键帧阶段。 |

---

## 13. 修订记录

| 日期 | 修订内容 |
|------|-----------|
| 2026-05-15 | 初版：基于对话 1～22 项确认定稿。 |
